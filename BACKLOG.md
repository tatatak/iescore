# iescore BACKLOG

将来やりたいが今すぐやらないこと。着手時に設計を再確認すること。

---

## リフォーム会社データを全国展開（S3 + Athena化）

**背景**
現在は神奈川県のみ（9,368社・2.1MB）を `public/data/reform_kanagawa.geojson` に置いてサーバーメモリでキャッシュしている。
47都道府県に広げると約30万件・30MBになり、Vercel のメモリ上限に当たる可能性がある。

**方針**
- データを S3 に CSV または Parquet で保存
- Vercel の API Route から AWS SDK 経由で Athena にクエリ
- `ST_Distance` で半径1.5km以内・最大30件を返す

**検討事項**
- Athena のクエリレイテンシが 2〜5秒かかる（チェックボックスONで数秒待つ使い方なので許容範囲か）
- BBSAC の Athena 環境（アカウント: 511586395504）を流用するか、iescore 用に独立させるか
- 代替案：Athena を使わず S3 から都道府県別 CSV を動的ロード（レイテンシ 0.5〜1秒・Athena より速い）

**データ追加手順（現行・都道府県ごと）**
1. 各都道府県の建設業許可業者名簿 Excel をダウンロード（各県庁サイトで公開）
2. `/tmp/geocode_reform.py` の `XLSX` と `OUT` パスを変更して実行（約30分/県）
3. GeoJSON を S3 にアップロード（全国化後は Parquet に変換推奨）
4. Athena テーブルを更新

**参考：現在の API Route**
`/app/api/reform/route.js` — ローカルファイルを読み込み haversine でフィルタ

---

## タウンライフ アフィリエイト追加（保留）

**背景**
注文住宅・土地の一括資料請求サービス。単価は2,000〜6,000円と住宅系では高め。

**保留理由**
- タウンライフ経由の資料請求後、ハウスメーカーから電話ラッシュがくるケースが多い
- ユーザーが「イエスコア経由で登録したら電話が来た」となると信頼を損なうリスクがある
- 注文住宅メインのため、マンション・中古戸建て検討者には関係ない
- アフィリエイト色が出やすく、今のチェックリスト（HOMES/モゲチェック/さくら事務所）のバランスを崩す

**着手する場合の方針**
戸建てモードのチェックリストに「土地・建築会社を探している方向け」として1項目のみ追加。
バナー広告的な使い方はしない。

---

## ~~マンション選択時の住所表示を番地レベルまで取得する~~（解決済み）

**背景**
現在の逆ジオコーディングは Nominatim（OpenStreetMap）を使用しており、日本では丁目レベル（例: 麻生区片平四丁目）までしか安定して取得できない。
日本の OSM には `addr:housenumber`（番地）のマッピングがほとんどなく、`house_number` フィールドは大半の地点で空になる。

番地が取得できないと Google Maps の「地図で見る」リンクが建物を特定できず、店舗入居ビルでは店舗内部のストリートビューが開いてしまう（既知・繰り返し発生）。

**候補API（2026-05-02 調査済み）**

| API | 番地取得 | 認証 | 備考 |
|-----|---------|------|------|
| 東京大学CSIS geocoder | ✗ | 不要 | フォワード（住所→座標）のみ。逆ジオコーディング非対応と確認済み |
| 国土地理院 逆ジオコーダー | ✗ | 不要 | 町丁目レベルまで。番地は返さない |
| Google Maps Geocoding API | ✓ | 要APIキー | 番地まで確実に返る。月 $200 無料枠（約40,000回/月）。採用最有力 |
| Jageocoder（OSS） | △ | 不要 | 自前サーバー＋20GB以上のDBが必要。Vercelでは運用困難 |

**実装方針**
Google Maps Geocoding API を Next.js API Route 経由で呼び出す。
- エンドポイント: `https://maps.googleapis.com/maps/api/geocode/json?latlng={lat},{lng}&language=ja&key=...`
- レスポンスの `formatted_address` または `address_components` から番地を取得
- APIキーは環境変数 `GOOGLE_GEOCODING_API_KEY` に保存
- マンション選択時のみ呼び出し（1回/選択）なので月40,000回は十分

**現在の実装**
`page.js` の `handleBuildingSelect` 内で Nominatim を呼び出し、`house_number` フィールドがあれば使う形になっている（データがない場合は丁目で止まる）。

---
