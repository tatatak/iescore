# イエスコア（iescore）プロジェクト

## サービス概要

**iescore.com**

不動産を買おうとしている一般市民（実需購入者）向けに、公的データだけを使って
エリアの価格・人口・リスクをスコア化して見せる中立な調査ツール。

> 「その価格、適正ですか？」

業者が知っていて買主が知らない情報格差を埋める。物件を売らない。利害関係ゼロ。

### 競合との差別化
- **楽待 賃貸経営マップ**: 不動産投資家向け。実需購入者には使いづらい
- **SUUMO / HOME'S**: 物件を売ることで収益。利益相反あり
- **イエスコア**: マイホーム購入検討者向け。公的データのみ。中立

---

## やること一覧

### STEP 1: 準備（今すぐできる）

- [x] `iescore.com` をConoHa WINGで取得済み
- [x] e-Statアカウント取得済み・APIキー（appId）取得済み
- [x] Mapbox アカウント作成・トークン取得済み
  - トークンは .env に保存すること（チャット・コードへの直書き禁止）
  - ドメイン制限を設定: https://iescore.com と http://localhost:3000
- [x] GitHubにリポジトリ作成済み → https://github.com/tatatak/iescore
- [x] Vercelアカウント作成・GitHubと連携済み（tatatak/iescore）

### STEP 2: 技術セットアップ

- [x] Next.js プロジェクト作成（Tailwind CSS込み）
- [x] Vercel にデプロイ・GitHub自動連携済み（iescore.vercel.app）
- [x] 環境変数登録済み（Vercel + .env.local）
  - NEXT_PUBLIC_MAPBOX_TOKEN ✅
  - ESTAT_APP_ID ✅
  - REINFOLIB_API_KEY ✅
- [x] 地図ライブラリ（Mapbox GL JS）を組み込む
- [x] Next.js API Routes でAPIキーをバックエンドに隠す

### STEP 3: データ接続

- [x] 国交省 不動産情報ライブラリ API 接続
  - 成約価格（中古マンション・宅地）表示 ✅
  - 政令指定都市の区レベル対応（18市・静的マッピング）✅
- [x] e-Stat API 接続
  - 人口推移グラフ（市区町村別・2010/2015/2020）✅
- [x] 国土地理院タイル接続（APIキー不要）
  - 洪水浸水想定 ✅
  - 土砂災害警戒区域 ✅
- [ ] 国土数値情報 GeoJSONダウンロード・DB化
  - 駅別乗降客数（S12）→ 利便性スコアの裏付けに使う

### STEP 4: 機能実装

- [x] 地図ベース画面（住所・駅名で検索）
- [x] エリアスコア表示（4指標）
  - 価格水準（成約価格の㎡単価平均）✅
  - 人口動向（2010→2020トレンド）✅
  - ハザードリスク（地図レイヤー切り替え）✅
  - 利便性（最寄り駅・スーパー・病院・学校レイヤー）✅
- [x] 人口推移グラフ（折れ線グラフ）
- [x] ハザードマップ切り替え表示（洪水・土砂）
- [x] 成約事例ドロワー（左スライドイン・詳細情報表示）
- [x] チェックリスト機能（購入前確認17項目）
- [x] 物件保存・比較機能（最大3件）
- [x] スマホ対応UI（ボトムタブバー）
- [x] 公示地価・路線価の推移グラフ（2022〜2026・REINFOLIB API）
- [x] 用途地域レイヤー（国土数値情報A29・地図に色分け表示）
- [x] マイホーム購入コスト診断（LoanSimulator）
  - 物件価格・諸費用・頭金・ローン返済額シミュレーション
  - 管理費・修繕積立金の適正判定
  - 年代別成約価格との比較（CondoPriceSimulator連動）
- [x] PDF レポート出力（新ウィンドウ印刷方式）
- [x] マンション名検索・ピン表示・物件サイト連携（HOMES・SUUMO・athome）
- [x] サービスロゴ表示（ヘッダー左）
- [ ] エリア比較機能（2エリアを並べて比較）

### STEP 4.5: 次に実装するデータ追加機能

優先度順。同じAPIキー・無料データのみ。

- [x] **公示地価の推移グラフ**（実装済み）
  - REINFOLIB API（`priceClassification=00`）・2022〜2026折れ線グラフ

- [ ] **駅乗降客数**（優先度：高）
  - 国土数値情報（S12）のGeoJSON静的ファイルをVercelに配置
  - 利便性スコアカードの「準備中」を実データに差し替え
  - 最寄り駅の乗降客数を表示し、★1〜5にスコア化

- [ ] **津波・高潮ハザードレイヤー**（優先度：中）
  - 国土地理院タイルに追加（タイルURL変更のみ・APIキー不要）
  - チェックリストに「津波浸水リスクを確認した」を追加

- [x] **用途地域レイヤー**（実装済み）
  - 国土数値情報GeoJSON（A29）・地図に色分け表示

- [ ] **住宅・土地統計調査**（優先度：低）
  - e-Stat API（既存キーで取得可能）
  - エリアの空き家率・持ち家率・平均築年数を表示
  - 人口動向カードに追記する形で実装

### STEP 5: コンテンツ

- [ ] リフォームコンテンツ（中古＋リフォームで「ちょうどいい家」）
  - リーズナブルな設備の組み合わせ実例
  - 費用の目安
  - 業者選びのポイント
- [ ] SEO対策記事
  - 「〇〇市 マンション 相場」系キーワード
  - 「〇〇駅 不動産 価格」系キーワード

### STEP 6: 収益化

- [ ] アフィリエイト登録（A8.net または バリューコマース）
  - 住宅ローン（報酬目安: 1件5,000〜20,000円）
  - 火災保険（報酬目安: 1件3,000〜8,000円）
  - 引越し見積もり（報酬目安: 1件500〜2,000円）
  - リフォーム会社見積もり（報酬目安: 1件3,000〜10,000円）
- [ ] Google Analytics 導入（Day1から必須）
- [ ] （将来）不動産会社との直接広告契約

---

## 既知の設計制約・禁止パターン

### Google Maps 連携：建物名のみの検索は使用禁止

**問題**: マンション名だけでGoogle Maps検索（`/maps/search/マンション名`）すると、テナント店舗が入っている建物の場合に**外観ではなく店舗内部のストリートビュー**が表示される。これは3回以上同じ問題が発生した既知の不具合。

**根本原因**: 建物名だけで検索するとGoogleが同名の店舗プレイスを拾ってしまう。

**解決済みの実装**: 「建物名 ＋ 市区名（丁目より前）」で検索する。
- Nominatim の逆ジオコーディング結果から 都道府県・丁目以降を除去した市区名を付加
- 例: `maps/search/パークタウン+さいたま市緑区東浦和` → 外観・ストリートビューが表示される

```javascript
const stripped = address
  .replace(/^.+?[都道府県]/, '')
  .replace(/[0-9０-９一二三四五六七八九十百千]+丁目.*/, '');
const url = `https://www.google.com/maps/search/${encodeURIComponent(name + ' ' + stripped)}`;
```

**禁止**: `https://www.google.com/maps/search/${建物名のみ}` 形式のURL生成（市区名なし）

---

## データソース一覧とライセンス

iescore は商用サービス（アフィリエイト収益モデル）のため、各データの商用利用可否を確認済み。

| データ | 入手先 | 商用利用 | 条件 | ファイル/API |
|---|---|---|---|---|
| 成約価格・地価公示 | 国交省 REINFOLIB | ✅ | 出典表示 | API |
| 人口推移・国勢調査 | e-Stat API（総務省） | ✅ | 出典表示 | API |
| ハザードマップ4種 | 国土地理院タイル | ✅ | 出典表示 | タイルURL |
| 地盤データ | 防災科研 J-SHIS | ✅ | 出典表示 | API |
| **医療機関** | 国土数値情報 P04（国交省）| ✅ | 出典表示 | `public/data/ksj-medical.json` |
| **幼稚園・こども園** | 国土数値情報 P29（国交省）| ✅ | 出典表示 | `public/data/ksj-kindergarten.json` |
| 用途地域 | 国土数値情報 A29（国交省）| ✅ | 出典表示 | GeoJSON |
| 住所検索 | 国土地理院 住所検索API | ✅ | 出典表示 | API |
| **最寄り駅** | HeartRails Express | ✅ | 「HeartRails Express」クレジット必須 | API |
| スーパー・コンビニ・バス停等 | OpenStreetMap（Overpass） | ✅ ODbL | 「© OpenStreetMap contributors」表示必須 | API |
| 地図タイル・ジオコーディング | Mapbox | ✅ | 商用プラン（有料）・帰属表示自動 | SDK |
| リフォーム会社 | 各都道府県 建設業許可業者名簿 | ✅ | 出典表示 | GeoJSON |

### 出典表示の実装状況

- **地図上**：Mapbox の `customAttribution` に「国土数値情報（国土交通省）・国土地理院・防災科研J-SHIS」を追加済み。Mapboxが「© Mapbox © OpenStreetMap contributors」を自動表示。
- **スコアパネル下部フッター**：全データソースを表示済み（`ScorePanel.js` 末尾）。

### 国土数値情報のデータ詳細

| ファイル | データ種別 | 年度 | 件数 | 備考 |
|---|---|---|---|---|
| `public/data/ksj-medical.json` | P04 医療機関 | 2020年 | 179,046件 | 病院・診療所・歯科 |
| `public/data/ksj-kindergarten.json` | P29 学校 | 2021年 | 15,660件 | 幼稚園・認定こども園のみ抽出 |

ダウンロード・変換スクリプト: `scripts/build-ksj-medical.py` / `scripts/build-ksj-kindergarten.py`

データ更新時は各スクリプトを再実行して `public/data/` を上書きし、`vercel --prod` でデプロイ。

### HeartRails の商用利用について（2026年5月確認）

https://express.heartrails.com/api.html より：
> 「APIは商用、非商用を問わず、無料でご利用になれます。無料でご利用になられる際には、アプリケーション内に『HeartRails Express』のクレジットを記載してください。」

→ **商用OK。クレジット表示のみ必須。** 大規模サイトになった場合は有料プランを検討。

---

## リフォーム会社データの作成方法

### データソース
各都道府県が公開している**建設業許可業者名簿**（Excelファイル）。無料・認証不要。

- 神奈川県の場合: 神奈川県 建設業課のページから `r0803bango.xlsx`（または最新版）をダウンロード
- 他県も「〇〇県 建設業許可 業者名簿 ダウンロード」で検索すると見つかる

### フィルタ条件
Excelの列構成（3行目からデータ）：
- 列B（index 1）: 業者名
- 列E（index 4）: 住所
- 列F（index 5）: 電話番号
- 列I（index 8）: 建築工事業（値 > 0 で許可あり）
- 列Z（index 25）: 内装仕上工事業（値 > 0 で許可あり）

→ 「内装仕上工事業 OR 建築工事業」の許可がある業者を抽出（神奈川: 9,368社）

### ジオコーディング方法
住所 → 緯度経度の変換に**国土地理院 住所検索API**を使用（無料・認証不要）：

```
https://msearch.gsi.go.jp/address-search/AddressSearch?q=神奈川県横浜市...
```

レスポンス例：
```json
[{"geometry": {"coordinates": [139.xxx, 35.xxx]}, ...}]
```

### ジオコーディングスクリプト
`/tmp/geocode_reform.py`（ローカルに保存済み）

```
入力:  /tmp/kanagawa_kensetsu.xlsx
出力:  public/data/reform_kanagawa.geojson
進捗:  /tmp/geocode_progress.json（中断・再開可能）
速度:  0.25秒/件（API過負荷防止）、100件ごとに保存
所要時間: 約30分（9,368件）
```

### 他都道府県を追加する場合
1. 各都道府県の建設業者名簿Excel（.xlsx）をダウンロード
2. スクリプトの `XLSX` と `OUT` パスを変更して実行
3. 生成した GeoJSON を `public/data/reform_{都道府県}.geojson` に保存
4. `/app/api/reform/route.js` で複数ファイルを結合してキャッシュする

### 注意事項
- `public/data/` に置くと外部から直接アクセス可能になる
- 本番前に `public/` 外（例: `src/data/`）に移動し、API Route 経由のみにすること
- APIルートは半径1.5km・最大30件に絞り込んで返す（全件返さない）

---

## 技術スタック

```
フロントエンド: Next.js（React）
地図:          Google Maps JavaScript API または Mapbox GL JS
ホスティング:  Vercel（無料枠）
バックエンド:  Next.js API Routes（APIキーをフロントに露出させない）
```

---

## 費用概算

```
ドメイン（iescore.com）: 2,068円/年
国交省API:               無料
e-Stat API:             無料
Google Maps / Mapbox:   月1万PV以下なら無料枠内
Vercel:                 無料枠内
Google Analytics:       無料

合計（立ち上げ時）: 約2,000円/年
```

---

## 将来の収益モデル

| フェーズ | 収益源 | 目安 |
|---|---|---|
| フェーズ1（〜月1万PV） | アフィリエイト | 月数万円 |
| フェーズ2（月5万PV〜） | 不動産会社との直接広告契約 | 月数十万円 |
| フェーズ3（月20万PV〜） | 投資家向けSaaS（月額課金） | 月数百万円 |

---

## 参考サイト

- 楽待 賃貸経営マップ: https://www.rakumachi.jp/property/land_price/map
- 全国地価マップ（chikamap）: https://www.chikamap.jp/
- 不動産情報ライブラリ: https://www.reinfolib.mlit.go.jp/
- e-Stat API: https://api.e-stat.go.jp/
- 国土数値情報: https://nlftp.mlit.go.jp/
- 国土地理院: https://maps.gsi.go.jp/
